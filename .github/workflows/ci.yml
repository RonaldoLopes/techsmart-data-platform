name: CI/CD TechSmart

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  # ---------------------------------------------------------- 1) Qualidade
  qualidade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install pyspark==3.5.0 pytest ruff

      - name: Lint
        run: ruff check .

      - name: Testes unitarios
        run: pytest tests/ -v

  # ---------------------------------------------------------- 2) Terraform
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - name: Formatacao
        run: terraform -chdir=infra fmt -check

      - name: Validacao
        run: |
          terraform -chdir=infra init -backend=false
          terraform -chdir=infra validate

  # ---------------------------------------------------------- 3) Deploy (desativado por enquanto)
  # Requer um Databricks Asset Bundle (arquivo databricks.yml na raiz do projeto),
  # que ainda nao foi configurado. Descomente quando o bundle estiver pronto.
  #
  # deploy:
  #   needs: [qualidade, terraform]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   env:
  #     DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
  #     DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: databricks/setup-cli@main
  #     - name: Validar o bundle
  #       run: databricks bundle validate -t prd
  #     - name: Publicar
  #       run: databricks bundle deploy -t prd